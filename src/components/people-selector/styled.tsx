import {colors} from '../../styles/colors';
import {constants} from '../../styles/constants';
import styled from '@emotion/styled';

export const PeopleSelectorWrapper = styled.div`
  display: flex;
  flex-direction: column;
`;
